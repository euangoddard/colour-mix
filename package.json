{
  "name": "colour-mix",
  "version": "1.0.0",
  "description": "Colour mixing tool",
  "main": "src/index.js",
  "scripts": {
    "start": "parcel ./src/index.html",
    "build": "parcel build ./src/index.html -d dist",
    "deploy":
      "rm -rf dist && npm run build && workbox injectManifest workbox-config.js && echo mix.euans.space > dist/CNAME && gh-pages -d dist"
  },
  "keywords": [],
  "author": "Euan Goddard",
  "license": "MIT",
  "devDependencies": {
    "gh-pages": "^1.1.0",
    "node-sass": "^4.9.0",
    "parcel-bundler": "^1.7.1",
    "typescript": "^2.8.3",
    "workbox-cli": "^3.1.0"
  }
}
